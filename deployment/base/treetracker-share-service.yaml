apiVersion: v1
kind: Service
metadata:
  name: treetracker-share-api
spec:
  selector:
    app: treetracker-share-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
