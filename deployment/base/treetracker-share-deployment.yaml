apiVersion: apps/v1
kind: Deployment
metadata:
  name: treetracker-share-api
  labels:
    app: treetracker-share-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: treetracker-share-api
  template:
    metadata:
      labels:
        app: treetracker-share-api
    spec:
      containers:
      - name: treetracker-share-api
        image: greenstand/treetracker-share-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: treetracker-api-database-connection
                key: db
          # - name: DATABASE_SCHEMA
          #   value: #schema-name#
          - name: NODE_LOG_LEVEL
            value: trace
          - name: REMOTE_LOG_URL
            value: http://104.131.78.177:8000
